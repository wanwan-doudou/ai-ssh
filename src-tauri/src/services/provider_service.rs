//! Provider 服务层 (占位)

pub struct ProviderService;

impl ProviderService {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ProviderService {
    fn default() -> Self {
        Self::new()
    }
}
