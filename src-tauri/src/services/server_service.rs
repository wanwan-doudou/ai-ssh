//! 服务器服务层 (占位)

pub struct ServerService;

impl ServerService {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ServerService {
    fn default() -> Self {
        Self::new()
    }
}
